# Ultraliser Python Bindings

This directory contains the scikit-build configuration for packaging the Ultraliser
pybind11 extension as a Python wheel. Use `pip install .` or `uv pip install .`
inside this folder to build and install the package.
